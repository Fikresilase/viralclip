📜 ViralClips Agent Rules (MANDATORY)
Environment

Python = 3.11 ONLY

Use PyQt6 ONLY — NEVER install or import PyQt5

Dependencies

Install strictly from:

pip install -r requirements.txt

When adding packages → pin versions in requirements.txt.

Core Stack (Do Not Change)

PyQt6 → UI

Gemini (google-generativeai) → AI

moviepy → video processing

opencv-python → face tracking

Absolute Don’ts

❌ Do NOT mix PyQt5 with PyQt6
❌ Do NOT change Python version
❌ Do NOT bypass requirements.txt

Run App
python main.py
